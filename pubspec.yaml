name: kfestival
description: "Global K-Art Platform powered by AI."
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & Utils
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  url_launcher: ^6.3.0
  
  # Firebase (Backend)
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.1
  firebase_auth: ^5.1.0
  firebase_storage: ^12.0.0
  
  # Location & Map
  google_maps_flutter: ^2.6.0
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  kpostal: ^1.1.0
  
  # AI & Media
  google_generative_ai: ^0.4.0
  flutter_tts: ^3.8.5
  
  # 🔥 [복구] 기존 코드가 깨지지 않도록 일단 살려둡니다.
  translator: ^1.0.0         

  # Media
  image_picker: ^1.1.1 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true